# Node.js Application Environment Variables
# WARNING: This is a test fixture with intentional misconfigurations!

# ❌ NODE_ENV should be 'production'
NODE_ENV=development

# ❌ Debug logging enabled
DEBUG=*
LOG_LEVEL=debug

# ❌ CORS wildcard - allows all origins
CORS_ORIGIN=*

# ❌ Weak API key
API_KEY=test123

# ❌ Weak secret
JWT_SECRET=changeme

# ❌ Weak database password
DB_PASSWORD=admin

# Database connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=myapp

# Server configuration
PORT=3000
